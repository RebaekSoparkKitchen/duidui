function proofRead(text: string, options: object) {
  const defaults = {
    space: true,
    punctuation: true,
    paragraph: false,
    duplicate: true,
    adKeyWords: true,
  };
  return text;
}
